import type { CapacitorConfig } from "@capacitor/cli"

const config: CapacitorConfig = {
  appId: "ec.pho3nixff.app",
  appName: "PHO3NIX",
  webDir: "dist",
  backgroundColor: "#050505",
  server: {
    androidScheme: "https",
  },
}

export default config